<section class="space-y-6">
  <div>
    <p class="eyebrow">Import / Export</p>
    <h1 class="text-3xl font-semibold">JSON pipeline placeholder</h1>
    <p class="mt-2 max-w-2xl text-[var(--muted)]">
      The validation schema is in place. The next step is wiring actual file upload and download flows
      to local data plus backend sync semantics.
    </p>
  </div>

  <div class="grid gap-4 md:grid-cols-2">
    <section class="card p-5">
      <h2 class="text-xl font-semibold">Export</h2>
      <p class="mt-2 text-[var(--muted)]">Serialize kits, sections, and items to a JSON backup format.</p>
    </section>

    <section class="card p-5">
      <h2 class="text-xl font-semibold">Import</h2>
      <p class="mt-2 text-[var(--muted)]">Validate incoming JSON and transform it into local entities.</p>
    </section>
  </div>
</section>

